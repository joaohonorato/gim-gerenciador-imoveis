datasources:
  default:
    url: jdbc:h2:mem:imoveis-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
    password: ""
    schema-generate: CREATE_DROP
    dialect: H2

app:
  test-support:
    enabled: true
